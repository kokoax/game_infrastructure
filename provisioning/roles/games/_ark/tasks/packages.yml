---
- name: Install packages
  yum:
    name: "{{ packages }}"
    state: installed
  vars:
    packages:
      - wget
      - rsync
      - gcc
      - glibc.i686
      - nc
      - tar
      - unzip
